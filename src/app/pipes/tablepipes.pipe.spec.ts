import { TablepipesPipe } from './tablepipes.pipe';

describe('TablepipesPipe', () => {
  it('create an instance', () => {
    const pipe = new TablepipesPipe();
    expect(pipe).toBeTruthy();
  });
});
